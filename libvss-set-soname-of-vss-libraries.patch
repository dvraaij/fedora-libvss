--- a/gnat/vss_gnat.gpr
+++ b/gnat/vss_gnat.gpr
@@ -14,6 +14,7 @@
 
    for Library_Name use "vss-gnat";
    for Library_Kind use VSS_Common.Library_Kind;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
--- a/gnat/vss_json.gpr
+++ b/gnat/vss_json.gpr
@@ -15,6 +15,7 @@
 
    for Library_Name use "vss-json";
    for Library_Kind use VSS_Common.Library_Kind;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
--- a/gnat/vss_regexp.gpr
+++ b/gnat/vss_regexp.gpr
@@ -15,6 +15,7 @@
 
    for Library_Name use "vss-regexp";
    for Library_Kind use VSS_Common.Library_Kind;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
--- a/gnat/vss_text.gpr
+++ b/gnat/vss_text.gpr
@@ -18,6 +18,7 @@
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    OS_Source_Dirs := ();
    OS_Library_Options := ();
--- a/gnat/vss_xml.gpr
+++ b/gnat/vss_xml.gpr
@@ -15,6 +15,7 @@
 
    for Library_Name use "vss-xml";
    for Library_Kind use VSS_Common.Library_Kind;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
--- a/gnat/vss_xml_matreshka.gpr
+++ b/gnat/vss_xml_matreshka.gpr
@@ -17,6 +17,7 @@
 
    for Library_Name use "vss-xml-matreshka";
    for Library_Kind use VSS_Common.Library_Kind;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
--- a/gnat/vss_xml_templates.gpr
+++ b/gnat/vss_xml_templates.gpr
@@ -15,6 +15,7 @@
 
    for Library_Name use "vss-xml-templates";
    for Library_Kind use VSS_Common.Library_Kind;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
--- a/gnat/vss_xml_xmlada.gpr
+++ b/gnat/vss_xml_xmlada.gpr
@@ -19,6 +19,7 @@
 
    for Library_Name use "vss-xml-xmlada";
    for Library_Kind use VSS_Common.Library_Kind;
+   for Library_Version use "lib" & Project'Library_Name & ".so." & external("VERSION");
 
    for Object_Dir use VSS_Common.Object_Dir;
    for Library_Dir use VSS_Common.Library_Dir;
